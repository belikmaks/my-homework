int main() {
  
  int n;    
  printf("Уведіть n: ");       //вводимо число, для якого будемо виводити таблицю множення
  scanf("%d\n", &n);

  printf("Таблиця множення для числа %d:\n", n);   //повідомлення про число для якого буде виводитися таблиця

    for (int i = 1; i <= 10; i++) {
        printf("%d * %d = %d\n", n, i, n * i);       // цикл for для виведення таблиці
    }feljfejofje
fff
    return 0;

}
